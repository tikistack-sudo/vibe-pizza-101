# code-and-crust-pizzeria

This repository contains a fun, colorful static site for **Code N Crust Pizzeria** — a cozy, modern pizza shop with a playful nod to Bitcoin and Bitcoin Pizza Day.

To view the site locally:

1. Open the file `index.html` in your browser. For example, from the repository root run:

```bash
# macOS / Linux
xdg-open index.html || open index.html

# or serve with a simple static server (recommended):
python3 -m http.server 8000
# then visit http://localhost:8000
```

Files added:

- `index.html` — main page
- `css/styles.css` — styles and theme

Enjoy the neon slices! 🍕 ₿